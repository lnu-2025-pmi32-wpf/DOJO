<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Presentation.ViewModels"
             x:Class="Presentation.Views.LoginPage"
             x:DataType="viewmodels:LoginViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#F5F5F5">

    <Grid>
        <ScrollView>
            <VerticalStackLayout 
                Padding="30,20"
                Spacing="10"
                VerticalOptions="Center"
                MaximumWidthRequest="500"
                HorizontalOptions="Center">

                <!-- Notification Message -->
                <Border 
                    IsVisible="{Binding NotificationMessage, Converter={StaticResource StringToBoolConverter}}"
                    BackgroundColor="{Binding IsNotificationSuccess, Converter={StaticResource BoolToNotificationColorConverter}}"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 8"
                    Padding="15,12"
                    Margin="0,0,0,10">
                    <Label 
                        Text="{Binding NotificationMessage}"
                        TextColor="White"
                        FontSize="13"
                        HorizontalTextAlignment="Center"/>
                </Border>

                <!-- Logo and Title -->
                <Label 
                    Text="🥋"
                    FontSize="60"
                    HorizontalOptions="Center"
                    Margin="0,0,0,5"/>

                <Label 
                    Text="DOJO"
                    FontSize="28"
                    FontAttributes="Bold"
                    HorizontalOptions="Center"
                    TextColor="#333333"/>

                <Label 
                    Text="Ваш персональний планувальник"
                    FontSize="14"
                    HorizontalOptions="Center"
                    TextColor="#666666"
                    Margin="0,0,0,20"/>

                <!-- Email Entry -->
                <Label 
                    Text="Email"
                    FontSize="11"
                    TextColor="#666666"
                    Margin="5,0,0,0"/>
                <Border 
                    BackgroundColor="White"
                    Stroke="{Binding EmailError, Converter={StaticResource ErrorToBorderColorConverter}}"
                    StrokeThickness="2"
                    StrokeShape="RoundRectangle 8"
                    MinimumWidthRequest="350">
                    <Entry 
                        Text="{Binding Email}"
                        Placeholder="example@mail.com"
                        Keyboard="Email"
                        FontSize="15"
                        HeightRequest="48"
                        VerticalTextAlignment="Center"
                        TextColor="#333333"/>
                </Border>

                <Label 
                    Text="{Binding EmailError}"
                    TextColor="Red"
                    FontSize="11"
                    HeightRequest="18"
                    IsVisible="{Binding EmailError, Converter={StaticResource StringToBoolConverter}}"
                    Margin="5,0,0,0"/>

                <!-- Password Entry -->
                <Label 
                    Text="Пароль"
                    FontSize="11"
                    TextColor="#666666"
                    Margin="5,0,0,0"/>
                <Border 
                    BackgroundColor="White"
                    Stroke="{Binding PasswordError, Converter={StaticResource ErrorToBorderColorConverter}}"
                    StrokeThickness="2"
                    StrokeShape="RoundRectangle 8"
                    MinimumWidthRequest="350">
                    <Entry 
                        Text="{Binding Password}"
                        Placeholder="••••••"
                        IsPassword="True"
                        FontSize="15"
                        HeightRequest="48"
                        VerticalTextAlignment="Center"
                        TextColor="#333333"/>
                </Border>

                <Label 
                    Text="{Binding PasswordError}"
                    TextColor="Red"
                    FontSize="11"
                    HeightRequest="18"
                    IsVisible="{Binding PasswordError, Converter={StaticResource StringToBoolConverter}}"
                    Margin="5,0,0,0"/>

                <!-- Login Button -->
                <Button 
                    Text="Увійти"
                    Command="{Binding LoginCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="0,8,0,0"/>

                <!-- Loading Indicator -->
                <ActivityIndicator 
                    IsRunning="{Binding IsLoading}"
                    IsVisible="{Binding IsLoading}"
                    Color="#4CAF50"
                    HeightRequest="30"/>

                <!-- Register Link -->
                <HorizontalStackLayout 
                    HorizontalOptions="Center"
                    Spacing="5"
                    Margin="0,15,0,0">
                    <Label 
                        Text="Ще не маєте акаунту?"
                        FontSize="13"
                        TextColor="#666666"/>
                    <Label 
                        Text="Зареєструватися"
                        Style="{StaticResource LinkButtonStyle}">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding RegisterCommand}"/>
                        </Label.GestureRecognizers>
                    </Label>
                </HorizontalStackLayout>

                <!-- Forgot Password Link -->
                <Label 
                    Text="Забули пароль?"
                    Style="{StaticResource LinkButtonStyle}"
                    HorizontalOptions="Center"
                    Margin="0,5,0,0">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ForgotPasswordCommand}"/>
                    </Label.GestureRecognizers>
                </Label>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>

