<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Presentation.Views.StatisticsPage"
             Title="Статистика"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout 
            Padding="20"
            Spacing="20"
            MaximumWidthRequest="1200"
            HorizontalOptions="Center">

            <Label 
                Text="📊 Статистика виконання"
                FontSize="28"
                FontAttributes="Bold"
                TextColor="#333333"
                HorizontalOptions="Center"
                Margin="0,10,0,10"/>

            <Border 
                BackgroundColor="{StaticResource SoftPink}"
                Stroke="{StaticResource HotPink}"
                StrokeThickness="2"
                Padding="20"
                StrokeShape="RoundRectangle 15"
                Margin="0,0,0,10">
                <Label 
                    Text="{Binding MotivationQuote}"
                    FontSize="18"
                    FontAttributes="Italic"
                    TextColor="{StaticResource DarkPink}"
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center"
                    LineBreakMode="WordWrap"/>
            </Border>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="20">
                
                <Border 
                    Grid.Column="0"
                    BackgroundColor="White"
                    Stroke="{StaticResource HotPink}"
                    StrokeThickness="2"
                    Padding="25"
                    StrokeShape="RoundRectangle 15">
                    <VerticalStackLayout Spacing="10">
                        <Label 
                            Text="✅"
                            FontSize="40"
                            HorizontalOptions="Center"/>
                        <Label 
                            Text="{Binding CompletedTasks}"
                            FontSize="48"
                            FontAttributes="Bold"
                            TextColor="{StaticResource HotPink}"
                            HorizontalOptions="Center"/>
                        <Label 
                            Text="Завершено"
                            FontSize="16"
                            TextColor="#666"
                            HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Border>

                <Border 
                    Grid.Column="1"
                    BackgroundColor="White"
                    Stroke="{StaticResource DeepPink}"
                    StrokeThickness="2"
                    Padding="25"
                    StrokeShape="RoundRectangle 15">
                    <VerticalStackLayout Spacing="10">
                        <Label 
                            Text="📋"
                            FontSize="40"
                            HorizontalOptions="Center"/>
                        <Label 
                            Text="{Binding TotalTasks}"
                            FontSize="48"
                            FontAttributes="Bold"
                            TextColor="{StaticResource DeepPink}"
                            HorizontalOptions="Center"/>
                        <Label 
                            Text="Всього завдань"
                            FontSize="16"
                            TextColor="#666"
                            HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Border>

            </Grid>

            <Border 
                BackgroundColor="White"
                Stroke="{StaticResource SoftPink}"
                StrokeThickness="1"
                Padding="25"
                StrokeShape="RoundRectangle 15">
                <VerticalStackLayout Spacing="15">
                    <Label 
                        Text="Рівень виконання"
                        FontSize="20"
                        FontAttributes="Bold"
                        TextColor="#333"/>

                    <HorizontalStackLayout Spacing="15" VerticalOptions="Center">
                        <Border 
                            BackgroundColor="{StaticResource SoftPink}"
                            Stroke="{StaticResource HotPink}"
                            StrokeThickness="2"
                            HorizontalOptions="FillAndExpand"
                            HeightRequest="35"
                            Padding="0"
                            StrokeShape="RoundRectangle 10">
                            <ProgressBar 
                                Progress="{Binding CompletionProgress}"
                                ProgressColor="{StaticResource DeepPink}"
                                HorizontalOptions="FillAndExpand"
                                VerticalOptions="Center"/>
                        </Border>
                        
                        <Label 
                            Text="{Binding CompletionRate}"
                            FontSize="24"
                            FontAttributes="Bold"
                            TextColor="{StaticResource DarkPink}"
                            VerticalOptions="Center"
                            WidthRequest="80"
                            HorizontalTextAlignment="End"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Border 
                BackgroundColor="White"
                Stroke="{StaticResource SoftPink}"
                StrokeThickness="1"
                Padding="25"
                StrokeShape="RoundRectangle 15">
                <VerticalStackLayout Spacing="20">
                    <Label 
                        Text="📈 Графік активності (останні 7 днів)"
                        FontSize="20"
                        FontAttributes="Bold"
                        TextColor="#333"/>

                    <Grid ColumnDefinitions="*,*,*,*,*,*,*" 
                          ColumnSpacing="10" 
                          HeightRequest="250"
                          VerticalOptions="End"
                          Padding="0,20,0,0">
                        
                        <VerticalStackLayout Grid.Column="0" Spacing="5" VerticalOptions="End">
                            <Border 
                                BackgroundColor="{StaticResource MediumPink}"
                                HeightRequest="{Binding Day1Height}"
                                WidthRequest="40"
                                StrokeShape="RoundRectangle 5,5,0,0"
                                VerticalOptions="End"/>
                            <Label 
                                Text="Пн"
                                FontSize="12"
                                TextColor="#666"
                                HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="1" Spacing="5" VerticalOptions="End">
                            <Border 
                                BackgroundColor="{StaticResource MediumPink}"
                                HeightRequest="{Binding Day2Height}"
                                WidthRequest="40"
                                StrokeShape="RoundRectangle 5,5,0,0"
                                VerticalOptions="End"/>
                            <Label 
                                Text="Вт"
                                FontSize="12"
                                TextColor="#666"
                                HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="2" Spacing="5" VerticalOptions="End">
                            <Border 
                                BackgroundColor="{StaticResource MediumPink}"
                                HeightRequest="{Binding Day3Height}"
                                WidthRequest="40"
                                StrokeShape="RoundRectangle 5,5,0,0"
                                VerticalOptions="End"/>
                            <Label 
                                Text="Ср"
                                FontSize="12"
                                TextColor="#666"
                                HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="3" Spacing="5" VerticalOptions="End">
                            <Border 
                                BackgroundColor="{StaticResource MediumPink}"
                                HeightRequest="{Binding Day4Height}"
                                WidthRequest="40"
                                StrokeShape="RoundRectangle 5,5,0,0"
                                VerticalOptions="End"/>
                            <Label 
                                Text="Чт"
                                FontSize="12"
                                TextColor="#666"
                                HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="4" Spacing="5" VerticalOptions="End">
                            <Border 
                                BackgroundColor="{StaticResource MediumPink}"
                                HeightRequest="{Binding Day5Height}"
                                WidthRequest="40"
                                StrokeShape="RoundRectangle 5,5,0,0"
                                VerticalOptions="End"/>
                            <Label 
                                Text="Пт"
                                FontSize="12"
                                TextColor="#666"
                                HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="5" Spacing="5" VerticalOptions="End">
                            <Border 
                                BackgroundColor="{StaticResource MediumPink}"
                                HeightRequest="{Binding Day6Height}"
                                WidthRequest="40"
                                StrokeShape="RoundRectangle 5,5,0,0"
                                VerticalOptions="End"/>
                            <Label 
                                Text="Сб"
                                FontSize="12"
                                TextColor="#666"
                                HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Column="6" Spacing="5" VerticalOptions="End">
                            <Border 
                                BackgroundColor="{StaticResource HotPink}"
                                HeightRequest="{Binding Day7Height}"
                                WidthRequest="40"
                                StrokeShape="RoundRectangle 5,5,0,0"
                                VerticalOptions="End"/>
                            <Label 
                                Text="Нд"
                                FontSize="12"
                                TextColor="{StaticResource HotPink}"
                                FontAttributes="Bold"
                                HorizontalOptions="Center"/>
                        </VerticalStackLayout>

                    </Grid>

                    <Label 
                        Text="💡 Висота стовпчика = кількість виконаних завдань"
                        FontSize="12"
                        TextColor="#999"
                        HorizontalOptions="Center"
                        Margin="0,10,0,0"/>
                </VerticalStackLayout>
            </Border>

            <Button 
                Text="🔄 Оновити статистику"
                Command="{Binding RefreshCommand}"
                BackgroundColor="{StaticResource DeepPink}"
                TextColor="White"
                FontSize="16"
                FontAttributes="Bold"
                CornerRadius="15"
                HeightRequest="50"
                Margin="0,10,0,20"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>